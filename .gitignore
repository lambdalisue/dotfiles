result

# Private binary-cache credentials — machine-local, never committed
home/config/nix/netrc

# git-wt / git worktree checkouts — machine-local, never committed
.worktrees/

# Codex (ChatGPT desktop) auto-generates these mirrors of CLAUDE.md and
# .claude/skills/. CLAUDE.md is the source of truth; keep the copies out of git.
/AGENTS.md
/.agents/

# OmniWM rescue copies: written next to settings.toml when the app cannot
# decode it (.corrupt*) or before a schema migration (.pre-v*). Transient.
home/config/omniwm/settings.toml.corrupt*
home/config/omniwm/settings.toml.pre-v*
