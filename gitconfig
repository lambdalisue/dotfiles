[include]
  path = ~/.gitconfig.local

[user]
  name  = Alisue
  email = lambdalisue@gmail.com
  signingkey = 74D5B4A7F3E1B28C

[rerere]
  enabled = true

[core]
  ignorecase = false
  quotepath = false

[color]
  ui = auto

[push]
  default = current

[branch]
  autosetupmerge = always

[filter "lfs"]
  required = true
  clean = git-lfs clean -- %f
  smudge = git-lfs smudge -- %f
  process = git-lfs filter-process

[commit]
  gpgsign = true
  verbose = true

[diff]
  algorithm = patience
  colormoved = dimmed-zebra

[alias]
  push-f = push --force-with-lease
  graph = log --graph --oneline --pretty=format:'%C(yellow)%h%Creset %s%C(auto)%d%Creset %C(green)%cr%Creset %C(magenta)<%an>%Creset'

[init]
  defaultBranch = main

[github]
  user = lambdalisue

[fetch]
  prune = true
