# Style of picker UI
picker:
  # Style of source picker
  source:
    style:
      widthRatio: 0.95
      widthMin: 80
      widthMax: 1000
      heightRatio: 0.80
      heightMin: 30
      heightMax: 1000
      previewRatio: 0.6

  # Style of action picker
  action:
    style:
      widthRatio: 0.5
      widthMin: 70
      widthMax: 700
      heightRatio: 0.5
      heightMin: 5
      heightMax: 200
      previewRatio: 0.65

  # Fallback style of source/action picker
  "":
    style:
      border: rounded
      divider: dashed
    query:
      headSymbol: ">"
      failSymbol: "x"

# Style of input UI
input:
  style:
    border: rounded
    divider: dashed
    widthRatio: 0.3
    widthMin: 10
    widthMax: 80

# JSON schema of this file
# yaml-language-server: $schema=https://raw.githubusercontent.com/lambdalisue/vim-fall/main/denops/fall/config/style/schema.json
$schema: https://raw.githubusercontent.com/lambdalisue/vim-fall/main/denops/fall/config/style/schema.json
