for filename in $(command find "${ZDOTDIR}" -follow -name "*.zwc"); do
  command rm -f $filename
done
if [[ -n $ZPLUGIN ]]; then
  for filename in $(command find "${ZPLUGIN}" -follow -name "*.zwc"); do
    command rm -f $filename
  done
fi
